import 'package:flutter/material.dart';

class AppColors {

  // store all the colors used inside the app here.s
  static const primaryColor = Color(0xff0FA956);
  static const ash = Color(0xff9B9B9B);
  static const white = Color(0xFFFFFFFF);
}